import { StyleSheet } from 'react-native';
const style = StyleSheet.create({
    container: {
        marginTop: '40%',
    },
    textField: {
        padding: 10,
    },
    inputBorder: {
        borderWidth: 1,
        borderColor: 'red',
    },
    buttonStyle : {
      color: 'white',
      marginTop: 50,
    },
})

export default style;